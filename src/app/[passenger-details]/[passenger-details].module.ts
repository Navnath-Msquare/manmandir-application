import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';

import { [passengerDetails]RoutingModule } from './[passenger-details]-routing.module';


@NgModule({
  declarations: [],
  imports: [
    CommonModule,
    [passengerDetails]RoutingModule
  ]
})
export class [passengerDetails]Module { }
