import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.msquare.karobooking',
  appName: 'Karobooking',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
